{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "API: migrate",
      "type": "shell",
      "command": "uv",
      "args": ["run", "--extra", "api", "alembic", "upgrade", "head"],
      "options": {
        "cwd": "${workspaceFolder}",
        "env": {
          "PYTHONPATH": "${workspaceFolder}/backend/src"
        }
      },
      "presentation": {
        "reveal": "always",
        "panel": "dedicated",
        "clear": true
      },
      "problemMatcher": []
    }
  ]
}
